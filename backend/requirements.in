# Direct dependencies for the ATS agent backend.
# Phase 2: resolve current compatible versions and pin in requirements.txt.
# Reference: design spec Section 15 — do not blindly copy stale pins.
# Updated: azure-functions is a CI-only dependency (pip install in workflow).

fastapi>=0.111.0
python-multipart>=0.0.9
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
pydantic-settings>=2.2.0

# Azure SDK
azure-identity>=1.16.0
azure-storage-blob>=12.20.0
azure-cosmos>=4.6.0
azure-servicebus>=7.12.0
azure-ai-documentintelligence>=1.0.0
azure-ai-translation-document>=1.0.0
azure-ai-textanalytics>=5.3.0
azure-ai-contentsafety>=1.0.0
azure-search-documents>=11.4.0
aiohttp>=3.9.0
redis>=5.0.0

# PDF fallback extraction
pypdf>=4.0.0

# OpenAI
openai>=1.51.0

# Observability
structlog>=24.1.0
azure-monitor-opentelemetry>=1.4.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
