# Azure Function trigger dependencies (separate from FastAPI container).
# Note: azure-functions is pre-installed in the Functions runtime — do not
# include it here or the Oryx remote build will fail.
azure-cosmos>=4.6.0
azure-servicebus>=7.12.0
azure-identity>=1.16.0
aiohttp>=3.9.0
pydantic>=2.7.1
pydantic-settings>=2.2.0
structlog>=24.1.0
